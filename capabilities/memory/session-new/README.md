# session-new
Create a fresh MAS session with a new identifier.

## Parameters
None.

## Returns
`SessionRecord` — the newly created session. The `id` field contains the
session ULID.

## Slash Command
`/new` (alias: `/fresh`) — start a fresh conversation session.
