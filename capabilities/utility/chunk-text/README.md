# chunk-text

Split text into ordered chunks.

## Parameters

| Name | Type | Required | Default | Description |
|------|------|----------|---------|-------------|
| `text` | `str` | yes | — | Text to chunk. |

## Returns

An array of chunk objects with `chunk_ordinal`, `text`, and `reference_range`.
