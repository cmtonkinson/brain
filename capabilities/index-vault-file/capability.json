{
  "capability_id": "index-vault-file",
  "kind": "pipeline_skill",
  "version": "1.0.0",
  "summary": "Generate and persist multi-chunk embeddings for one vault file.",
  "enabled": false,
  "autonomy": 0,
  "requires_approval": false,
  "side_effects": ["writes_embeddings"],
  "input_schema": {
    "file_path": "string | The vault-relative path of the document to index."
  },
  "output_schema": {
    "embedding_count": "integer | The number of embedding vectors persisted."
  },
  "pipeline": [
    "vault-get-file",
    {
      "capability": "chunk-text",
      "input_mapping": {
        "text": "content"
      }
    },
    "language-model-embed-chunks",
    "embedding-upsert-document-batch"
  ]
}
