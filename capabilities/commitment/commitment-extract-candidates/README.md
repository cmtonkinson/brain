# commitment-extract-candidates
Native Op that invokes `service_commitment.extract_commitment_candidates` to scan arbitrary text for commitment signals and return zero or more structured candidates with confidence scores.
