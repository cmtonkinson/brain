# code-task-status
Return the current lineage row for one Software task.

------------------------------------------------------------------------
## Inputs
- `task_id`: the task identifier to inspect.

------------------------------------------------------------------------
## Output
The current `Task` lineage row.

------------------------------------------------------------------------
## Effect/Approval
This op is classified `(read, never)`.

------------------------------------------------------------------------
_End of code-task-status_
