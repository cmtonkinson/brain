# code-workspace-list
List registered Software workspaces.

------------------------------------------------------------------------
## Inputs
- `include_revoked` (optional): if true, also return revoked workspaces. Defaults to false.

------------------------------------------------------------------------
## Output
Array of `Workspace` records.

------------------------------------------------------------------------
## Effect/Approval
This op is classified `(read, never)`.

------------------------------------------------------------------------
_End of code-workspace-list_
