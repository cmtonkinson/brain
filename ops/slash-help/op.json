{
  "op_id": "slash-help",
  "kind": "logic",
  "version": "1.0.0",
  "summary": "List all slash commands, or describe one in detail when a query is given.",
  "enabled": true,
  "input_schema": {
    "query": "string | optional | When omitted, list all slash commands. When given, exact-match on slash name, alias, or op_id returns details for that op; substring-match returns the matching subset (or details if exactly one matches)."
  },
  "output_schema": "string | Formatted slash-command listing or per-op detail.",
  "simple_output_path": "result",
  "slash_command": {
    "name": "help",
    "description": "list available slash commands"
  },
  "effect": "read",
  "approval": "never"
}
