# commitment-run-miss-detection
Native Op that invokes `service_commitment.run_miss_detection` to mark due open commitments as `MISSED` and route any resulting notifications through the Commitment Service's owned orchestration.
