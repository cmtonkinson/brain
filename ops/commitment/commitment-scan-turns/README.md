# commitment-scan-turns
Native Op that invokes `service_commitment.run_turn_scanner` to scan recent inbound Recall turns for commitment candidates and feed them through the existing creation and dedup pipeline.
