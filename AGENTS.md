# AGENTS.md
Instructions for agents working in this repo:
- Keep `utcp.sample.json` in sync with `~/.config/brain/utcp.json` whenever you update UTCP/Code-Mode config.
- Run tests (`./test.sh`) after making changes to validate behavior.
